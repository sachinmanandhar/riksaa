# from rest_framework.routers import DefaultRouter
# from  map_app import urls as map_app_urls

# app_name = "default_routers"

# routers = DefaultRouter()

# # * Always extend urls after registry. If you register urls before registry, it will now be available in browsable api
# # Extend router's registry
# # routers.registry.extend(map_app_urls.router.registry)

# # Extend Router's urls
# routers.urls.extend(map_app_urls.urlpatterns)

# urlpatterns = routers.urls# * Always extend urls after registry. If you register urls before registry, it will now be available in browsable api
